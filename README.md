# FileManager

FileManager — это простая утилита для быстрого создания, удаления и переименования файлов и папок (включая вложенные).

## Шаблоны

В контексте этой утилиты шаблон — это строка, содержащая символ(ы) `*`. Такой символ означает любой непустой набор символов (любая строка длиной больше нуля).

Наглядные примеры шаблонов:

- `*.txt` — соответствует: `Hello.txt`, `lol3.txt`, `five.user.txt`, `a.txt`
- НЕ соответствует: `.txt`

Вы можете использовать `*` в любом месте шаблона и любое количество раз. Все такие шаблоны будут корректно распознаны FileManager.

## Команды

### 1. Создание файлов
Создает указанное количество файлов по указанному пути:

1. Введите путь к папке, в которой создадутся файлы
2. Введите имя файлов (одинаковое для всех)
3. Укажите количество создаваемых файлов

### 2. Переименование
Переименовывает файлы и папки в указанной директории и её подпапках:

1. Введите путь к папке, внутри которой будет происходить переименование
2. Введите новое имя (одинаковое для всех элементов)
3. Введите список имён и шаблонов (для файлов и папок, к которым нужно применить функцию). Завершите список точкой — `.`
4. Введите список исключений (файлы и папки, которые не будут затронуты). Завершите список точкой — `.`

### 3. Удаление
Удаляет файлы и папки по указанному пути:

1. Введите путь к папке (или файлу), из которой нужно удалять элементы
2. Введите список имён и шаблонов (для файлов и папок, которые нужно удалить). Завершите список точкой — `.`
3. Введите список исключений (файлы и папки, которые не будут удалены). Завершите список точкой — `.`

### 4. Поиск файлов и папок
Находит и отображает файлы и папки по указанным шаблонам:

1. Введите путь к папке, в которой нужно выполнить поиск
2. Введите список имён и шаблонов для поиска. Завершите список точкой — `.`
3. Введите список исключений (файлы и папки, которые не будут отображены). Завершите список точкой — `.`

### 5. Управление флагами
Позволяет тонко настроить поведение программы с помощью специальных флагов:

- **Рекурсивный обход**
  - `-rec` - включает рекурсивный обход всех вложенных папок
  - `-nrec` - выключает рекурсивный обход (работа только с текущей папкой)

- **Режимы удаления**
  - `-deld` - удаляются только папки
  - `-delf` - удаляются только файлы  
  - `-delfd` - удаляются и папки, и файлы

- **Режимы создания**
  - `-cref` - создаются файлы
  - `-cred` - создаются папки

- **Режимы переименования**
  - `-rend` - переименовывает только папки
  - `-renf` - переименовывает только файлы
  - `-renfd` - переименовывает и файлы, и папки

- **Учет регистра**
  - `-reg` - учитывается регистр при проверках имен файлов
  - `-nreg` - регистр не учитывается

- **Работа с корневой папкой**
  - `-root` - операции работают с корневой папкой
  - `-nroot` - операции не затрагивают корневую папку

### 6. Просмотр информации о флагах
Позволяет посмотреть:
- Подробное описание всех доступных флагов
- Текущее состояние флагов в программе

### 7. Выход
Завершает работу программы.

## Примеры использования

### Создание нескольких файлов
Выберите команду: 1
Введите путь: C:\my_documents
Введите имя создаваемого объекта: report.txt
Введите количество создаваемых файлов: 5


### Рекурсивное переименование картинок
Выберите команду: 2
Введите путь: C:\photos
Введите новое имя объекта/объектов: vacation
Введите список файлов для переименования: .jpg
.
Введите список исключений: backup.
.


### Удаление временных файлов
Выберите команду: 3
Введите путь: C:\work_folder
Введите список ключевых слов для удаления: temp*
*.tmp
.
Введите список исключений: important.tmp
.


### Поиск документов
Выберите команду: 4
Введите путь: C:\projects
Введите список ключевых слов для поиска: .docx
.pdf
readme.
.
Введите список исключений: draft
.


## Важные примечания

- Программа автоматически обрабатывает пути в кавычках
- Пути не должны содержать русские символы во избежание проблем с кодировкой
- Поддерживаются все стандартные символы в именах файлов, кроме запрещённых Windows: `\`, `/`, `:`, `*`, `?`, `"`, `<`, `>`, `|`
- При переименовании нескольких элементов программа автоматически добавляет нумерацию
- Все операции можно отменить, указав нужные файлы в списке исключений

## Сборка через CMake

```bash
cd путь_к_папке_с_проектом
mkdir build
cd build
cmake ..
cmake --build . --target ALL_BUILD --config Release -j